// document.querySelector('button').addEventListener('click', survey)

// const survey = () => {

// };

// let form = document.getElementById("contact-form");

// form.addEventListener("submit", function (e) {
//   e.preventDefault();
//   let name = document.getElementById("name").value;
//   let email = document.getElementById("email").value;
//   let message = document.getElementById("message").value;

//   alert(
//     `Thank you ${name} for reaching out to us! We will respond to your email ${email} as soon as possible.`
//   );
// });

// // add class to the about section
// aboutSection.classList.add("fade-in");

// var aboutSection = document.getElementById("about");

// window.addEventListener("scroll", function () {
//   var bounding = aboutSection.getBoundingClientRect();
//   if (
//     bounding.top >= 0 &&
//     bounding.left >= 0 &&
//     bounding.right <=
//       (window.innerWidth || document.documentElement.clientWidth) &&
//     bounding.bottom <=
//       (window.innerHeight || document.documentElement.clientHeight)
//   ) {
//     aboutSection.classList.add("animated");
//   }
// });
